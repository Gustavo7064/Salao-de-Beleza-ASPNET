@model List<SalaoDeBeleza.Models.Profissional>

@{
    ViewData["Title"] = "Profissionais";
}

<div class="card">
    <div class="card-header">
        <div class="d-flex justify-content-between align-items-center">
            <h5>Lista de Profissionais</h5>
            <a asp-action="Criar" class="btn btn-sm btn-primary"><i class="fas fa-plus"></i> Novo Profissional</a>
        </div>
    </div>
    <div class="card-body">
        @if (ViewBag.Sucesso != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle"></i> @ViewBag.Sucesso
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        @if (Model.Count == 0)
        {
            <div class="text-center py-5">
                <i class="fas fa-inbox" style="font-size: 3rem; color: #ccc;"></i>
                <p class="mt-3 text-muted">Nenhum profissional cadastrado</p>
                <a asp-action="Criar" class="btn btn-primary"><i class="fas fa-plus"></i> Cadastrar Primeiro</a>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Especialidade</th>
                            <th>CPF</th>
                            <th>Status</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var prof in Model)
                        {
                            <tr>
                                <td>
                                    @if (!string.IsNullOrEmpty(prof.Foto))
                                    {
                                        <img src="~/img/fotos/@prof.Foto" alt="Foto" style="width: 30px; height: 30px; border-radius: 50%; margin-right: 10px; object-fit: cover;" />
                                    }
                                    else
                                    {
                                        <i class="fas fa-user-circle" style="font-size: 1.5rem; margin-right: 10px; color: #ccc;"></i>
                                    }
                                    @prof.Nome
                                </td>
                                <td>@prof.Especialidade</td>
                                <td>@prof.Cpf</td>
                                <td>
                                    @if (prof.Ativo)
                                    {
                                        <span class="badge badge-success"><i class="fas fa-check"></i> Ativo</span>
                                    }
                                    else
                                    {
                                        <span class="badge badge-danger"><i class="fas fa-times"></i> Inativo</span>
                                    }
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <a asp-action="Editar" asp-route-id="@prof.IdProfissional" class="btn btn-sm btn-primary"><i class="fas fa-edit"></i></a>
                                        @if (prof.Ativo)
                                        {
                                            <form asp-action="Excluir" asp-route-id="@prof.IdProfissional" method="post" style="display: inline;" onsubmit="return confirm('Desativar este profissional?');">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
